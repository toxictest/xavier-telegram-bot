services:
  - type: web
    name: xavier-telegram-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python webhook_bot.py
    healthCheckPath: /health
